# React Native PlayGround

Simple Card App for studying

## Test

`yarn install && yarn start`

## npm version

Compatible with `5.5.1`.

## Note

- Not use `Redux` **Intentionally**.
- Button does not work well with FlipCard.
- Async calls can be improved if using `redux-saga` or `dva`